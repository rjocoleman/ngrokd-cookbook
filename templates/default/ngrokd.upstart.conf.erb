description 'ngrokd'

start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

respawn

exec /usr/bin/ngrokd -tlsKey="<%= @tlskey %>" -tlsCrt="<%= @tlscrt %>" -domain="<%= @domain %>" -log="<%= @log %>"
